import { makeStyles } from "@material-ui/core";

export default makeStyles(theme => ({
  productsArea: {
    marginTop: 48
  },
  productsTitle: {
    fontSize: "1.3rem",
    fontWeight: "500",
    color: "var(--pcolor)"
  }
}));
